expected_extractor_notices = ['ComposerLockVersion(ComposerPluginApiVersionConfined("2.2.0", "~2.2.0"))']
description = "Tests whether for Composer < 2.3, Composer ~2.2.0 instead of ^2 is used, due to plugin BC breaks in 2.3"
